#include "script_component.hpp"

// Editor post-init — keybind already registered via initSettings
//
// Admins can apply patch rules to an object under crosshair with:
//   [] call a3sql_patch_editor_fnc_applyToTarget;
// Or bind to a key in CBA keybind settings.
