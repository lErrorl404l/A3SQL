[project]
name = "a3sql-tools"
version = "0.1.0"
description = "A3SQL development tooling (SQF validators, Steam/Proton helpers)"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta:__legacy__"

[tool.uv]
package = false
