//! Binary save/load — serialize and deserialize the full database state.
//! Uses a simple protobuf-like binary format for fast I/O.

// Database save/load/backup — pending extraction from lib.rs or serialize
